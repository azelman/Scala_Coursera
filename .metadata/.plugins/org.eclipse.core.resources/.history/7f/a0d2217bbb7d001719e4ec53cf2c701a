object week62 {
  
  var map = "aaabbc" groupBy (x => x)             //> res0: scala.collection.immutable.Map[Char,String] = Map(b -> bb, a -> aaa, c 
	for ((x,y) <- map) yield (x, y.length)           //| -> c)
}