object week61 {
  println("Welcome to the Scala worksheet")       //> Welcome to the Scala worksheet
  
  val col = 2
  val row = 2
  for {
  	c <- 0 until col
  	r <- 0 until row
  	if (c - col == r - row) || (c + col == r + row)
  	} yield (c, r)
}