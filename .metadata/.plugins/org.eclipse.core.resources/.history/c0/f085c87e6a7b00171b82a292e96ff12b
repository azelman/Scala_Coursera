object week61 {
  println("Welcome to the Scala worksheet")       //> Welcome to the Scala worksheet
  
  val col = 1                                     //> col  : Int = 1
  val row = 2                                     //> row  : Int = 2
  val n = 4                                       //> n  : Int = 4
  
  val queens = Vector(2,0)                        //> queens  : scala.collection.immutable.Vector[Int] = Vector(2, 0)
  
  def diags =
  for {
  	c <- 0 until n
  	r <- 0 until n
  	if (c - col == r - row) || (c + col == r + row)
  	} yield (c, r)                            //> diags: => scala.collection.immutable.IndexedSeq[(Int, Int)]
 
  queens.reverse                                  //> res0: scala.collection.immutable.Vector[Int] = Vector(0, 2)
}